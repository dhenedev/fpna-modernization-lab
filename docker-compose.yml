services:
  fpna-modernization-lab:
    build: .
    container_name: fpna-modernization-lab
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    command: streamlit run streamlit/app.py --server.address=0.0.0.0